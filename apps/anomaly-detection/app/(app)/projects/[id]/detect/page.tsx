import { PlaceholderTab } from "../_PlaceholderTab";

export default function DetectPage() {
  return (
    <PlaceholderTab
      title="Detect"
      description="Score new images against the trained centroid."
      body="The detection flow with heatmap overlay lands here in Phase 6."
    />
  );
}
