---
title: RAG support chatbot grounded in your own documents
summary: A streaming AI assistant that answers only from indexed source files, cites the file each answer came from, and says "I don't know" when retrieval is empty.
client: Portfolio demo
stack:
  - Next.js 16
  - OpenAI gpt-4o-mini
  - Pinecone
  - Neon Postgres
publishedAt: 2026-05-04
---

For support teams whose answers already live in their own docs but cost hours to dig out manually. Built as the smallest RAG pipeline that survives production: ingest, retrieve, cite, refuse on empty. The engineering work after this is tuning, not architecture.

## Pipeline

```mermaid
flowchart LR
  A[Admin uploads<br/>PDF / TXT / MD] --> B[Chunk + embed<br/>1024-dim]
  B --> C[(Pinecone)]
  D[User question] --> E[Top-5 retrieval]
  C --> E
  E --> F[Grounded prompt<br/>with citations]
  F --> G[Streaming answer<br/>via Vercel AI SDK]
```

## What's built

- **Drag-and-drop ingestion.** Admin uploads a file; server parses, chunks, embeds, writes vectors to Pinecone with the source filename in metadata. Postgres tracks chunk counts; delete cascades both stores in one server action.
- **Top-5 retrieval per message.** Matching chunks land in the system prompt under `## Knowledge base`, prefixed `[1] (from: returns-policy.pdf)` so the model cites naturally.
- **Streaming chat UI** via the Vercel AI SDK.
- **"Don't know" is the default.** Empty retrieval strips the knowledge-base block; the persona's refusal rule takes over instead of letting the model confabulate.
- **Admin console at `/admin`** with chunk counts, delete, and upload-type validation.

## Tradeoffs

- **Top-5 retrieval, no rerank or query rewrite.** Cheap to debug and predictable per question; gives up recall on multi-hop questions where the relevant chunks aren't surfaced by the user's exact phrasing.
- **Filename citations, not page numbers.** Fine for short policies, weaker on long PDFs where readers want a page jump. Page-level metadata is a one-line ingestion change when the docs justify it.
- **Refusal is the default on empty retrieval, not a fallback.** Hallucinated policy is worse than a polite "I don't know". This is chosen explicitly so the persona never quietly drops back to the model's general training.

[Live demo →](/ai-chatbot)
