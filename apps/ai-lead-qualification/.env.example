# SQLite by default for zero-config demo. Swap to a Postgres URL for production.
DATABASE_URL="file:./dev.db"

# Optional. The app falls back to deterministic stub qualifications when unset,
# so you can demo the full UI without burning OpenAI tokens.
OPENAI_API_KEY=