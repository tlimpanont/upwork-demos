import { PlaceholderTab } from "../_PlaceholderTab";

export default function ReportsPage() {
  return (
    <PlaceholderTab
      title="Reports"
      description="Export detection runs as JSON, CSV, or PDF."
      body="The export pipeline lands here in Phase 7."
    />
  );
}
