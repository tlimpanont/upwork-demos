import { PlaceholderTab } from "../_PlaceholderTab";

export default function TrainPage() {
  return (
    <PlaceholderTab
      title="Train"
      description="Compute embeddings, centroid, and threshold."
      body="The training pipeline lands here in Phase 5."
    />
  );
}
